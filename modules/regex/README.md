# Regular Expressions

Pattern matching with regex for emails and zip codes.

## Key concepts

- Pattern and Matcher
- matches() and find()
- Capturing groups

## Run

```bash
cd modules/regex
mvn exec:java -Dexec.mainClass=com.mathffreitas.regex.Main
```
