package com.mathffreitas.strings;

public class Final {
    public static void main(String[] args) {
    // final (class) -> não pode ser herdado
    // final (properties) -> indica que vai ser definido no constructor e não será mais alterado
    // final (object) -> indica que não pode ser modificada depois de definida
}
}
