# Date and Time

The java.time API for dates, times, and formatting.

## Key concepts

- LocalDate, LocalTime, LocalDateTime
- DateTimeFormatter
- Parsing and formatting

## Run

```bash
cd modules/date-time
mvn exec:java -Dexec.mainClass=com.mathffreitas.datetime.Main
```
